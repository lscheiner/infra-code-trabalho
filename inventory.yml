all:
   vars:
      ansible_user: 'vagrant'
      ansible_become: 'yes'
      ansible_become_method: 'sudo'
      slacko_path:  '/opt/install4'

local:
   hosts:
     10.0.0.10:
        datacenter: primario
remoto:
  hosts:
    10.0.0.11:
       datacenter: secundario